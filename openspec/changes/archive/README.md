# Completed changes

One directory per shipped change, moved here by the archive step after its delta was
folded into `openspec/specs/`. Kept so the history of *why* a requirement exists stays
readable.
